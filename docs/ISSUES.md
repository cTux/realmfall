# ISSUES

* After log windows initialization we can see first three messages (welcome + rumor + motd) with "Day 1, 00:00" timestamp, not the current (saved) time in the game
* Hex info window:
    * if there are no enemies - we don't need to see "enemies 0" text here
* Debugger window:
    * there should not be "trigger earthquake" button
* There are variables in the code which are named wrong (doesn't represent its value)
* Add game genre tags into README.md file
