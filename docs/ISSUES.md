# ISSUES

* Sometimes top-left to bottom-right visual "diagonal" appear that removes fog of war from hidden hexes, [image](./issues/top-left-to-bottom-right-visual-diagonal-glitch.jpg).
* Every time world is loaded I see first render of the world map and then everything jumps (hexes and icons become bigger, etc.) and world is rendered in another way (I assume because of the browser's window size recalculations or smth like that).
* Sometimes tooltip is not shown while hovering the hex. Moving mouse around and returning it back to this hex fixes the issue, tooltip is shown. Need to fix the root cause.
