import type { CSSProperties } from 'react';

export const HEX_INFO_CONTENT_SLOT_STYLE = Object.freeze({
  '--slot-size': '54.4px',
  '--slot-icon-size': '1.76rem',
} as CSSProperties);

export const HEX_INFO_COMPACT_CONTENT_SLOT_STYLE = Object.freeze({
  '--slot-size': '27.2px',
  '--slot-icon-size': '24.48px',
} as CSSProperties);
